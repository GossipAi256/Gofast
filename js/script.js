// Placeholder for future JS functionality
console.log("Gofast Car Bond website loaded");

// Example: Alert when someone clicks a call button
document.querySelectorAll('.car-item p').forEach(item => {
    item.addEventListener('click', () => {
        console.log("Contact info clicked");
    });
});